# Default disabled input templates
# Configure inputs via the Splunk UI (Configuration > Inputs)

[openai_usage_logs]
python.version = python3
interval = 3600
start_by_shell = false

[openai_audit_logs]
python.version = python3
interval = 300
start_by_shell = false
